# Asyn_FIFO
Asynchronous FIFO in Verilog with Gray code CDC synchronization, verified using a CDC-aware class-based SystemVerilog CRV testbench with separate write/read clock domain drivers. UVM upgrade in progress.
